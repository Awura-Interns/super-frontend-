import { Outlet } from 'react-router-dom';
import "../Assets/styles/SharedCartLayout.css"
const Cart = () => {
    return (
        <><section>

          
         


            <Outlet />
        </section>

        </>
    );
};
export default Cart;
